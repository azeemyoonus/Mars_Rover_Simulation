/**
 * Interface representing a component that can display the position on a grid.
 */
public interface GridComponent {
    /**
     * Displays the current position on the grid.
     */
    void displayPosition();
}
